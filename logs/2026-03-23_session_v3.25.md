# 세션 로그 — 2026-03-23 (v3.25 개선)

**작업자**: Claude Sonnet 4.6 (AI)
**날짜**: 2026-03-23
**세션 목표**: currentSituation.md 의존도 제거, 헤딩 비율 기준 99% 상향, BM25 캐시 즉시 무효화, check_quality.py EditAgentLog 노출, 매뉴얼 v3.25 전면 갱신, refined_vault 문서 정제

---

## 변경 파일 목록

### src/ (TypeScript)

| 파일 | 변경 내용 |
|------|-----------|
| `src/lib/tfidfCache.ts` | `invalidateTfIdfCache(vaultPath)` 신규 추가 — Edit Agent 파일 저장 직후 IndexedDB BM25 캐시 무효화 |
| `src/services/editAgentRunner.ts` | `invalidateTfIdfCache` import + 파일 저장 성공 시 즉시 호출; `runQualityCheck` import + 사이클 완료 후 자동 실행 |
| `src/services/syncRunner.ts` | `runScript` 반환 타입 수정; `getScriptAPI()` 헬퍼 추출; `runQualityCheck()` export 신규 추가 |
| `src/lib/graphRAG.ts` | `situationPrefix` 블록 전체 삭제 (~22줄) — currentSituation.md 강제 주입 완전 폐지 |

### manual/ (매뉴얼 v3.25)

| 파일 | 변경 내용 |
|------|-----------|
| `manual/sections/s01_overview.md` | KPI 표 헤딩 비율 `80%→99%` 수정 |
| `manual/sections/s06_optimization.md` | 헤딩 보유 비율 기준 `80%→99%` 수정 |
| `manual/sections/s07_quality.md` | §13 Edit Agent 자동 실행 안내 추가; §14.1 currentSituation 일반 허브로 재작성; §16 체크리스트 `99%` 갱신 |
| `manual/sections/s08_operations.md` | §17.1 신규 문서 6단계 currentSituation 제거; §17.1.3 v3.25 변경 재작성; §17.1.5 Edit Agent 즉시 캐시 무효화 행 추가; §17.2 주간 의무 갱신 폐지; §17.3 스크립트 표 갱신 |
| `manual/sections/s09_troubleshoot.md` | §18.1 BFS 오염 행 취소선; §18.2 currentSituation 강제화 폐지 안내; §18.3/4/5 currentSituation 항목 제거; §19.3 v3.25 버전 갱신; 변경이력 v3.25 추가 |
| `manual/00_index.md` | v3.25 링크·설명 갱신 |

### tools/ & manual/scripts/ (Python)

| 파일 | 변경 내용 |
|------|-----------|
| `tools/check_quality.py` | 헤딩 WARN 기준 `< 0.2 → < 0.01` (99% 기준) |
| `manual/scripts/check_quality.py` | 동일 |
| `tools/enhance_wikilinks.py` | line 183 버그 수정: `re.search(title, ...)` → `re.search(re.escape(title), ...)` |
| `tools/strengthen_links.py` | line 119 prefix 매칭 명확화: `stem.startswith(s[:len(stem)])` → `stem.startswith(s)` |
| `refined_vault/.manual/scripts/enhance_wikilinks.py` | 동일 버그 수정 (별도 복사본) |
| `refined_vault/.manual/scripts/gen_index.py` | `> ⚠️ 이 문서를 먼저 읽을 것` 배너 생성 코드 제거 |

### refined_vault/ (볼트 문서)

| 작업 | 내용 |
|------|------|
| `currentSituation.md` 재생성 | gen_index.py 재실행 → 배너 제거 + 최신 데이터 반영 (2026-02-27, 2026-03-23) |
| 헤딩 없는 파일 일괄 수정 | 86개 파일에 `## 개요` 섹션 자동 삽입 → 0개 달성 |
| `enhance_wikilinks.py` 실행 | 링크 없는 127개 파일에 wikilink 주입 |
| `audit_and_fix.py` 3차 실행 | 중첩 wikilink 138건 → 79건 → 최종 8건 잔존 |
| `check_links.py` 결과 | 깨진 링크 19개 잔존 (참조 파일 `626391685_...` 없음 — 수동 조치 필요) |

---

## 실행 커맨드 이력

```bash
# gen_index.py 재실행 (배너 제거 후)
python -X utf8 refined_vault/.manual/scripts/gen_index.py refined_vault/active

# 헤딩 없는 파일 일괄 삽입 (인라인 Python)
python -X utf8 -c "
import re
from pathlib import Path
active = Path('refined_vault/active')
count = 0
for md in active.glob('*.md'):
    ...  # ## 개요 삽입 로직
"

# enhance_wikilinks.py 실행 (버그 수정 후)
python -X utf8 refined_vault/.manual/scripts/enhance_wikilinks.py refined_vault/active

# audit_and_fix.py 3차 실행
python -X utf8 tools/audit_and_fix.py refined_vault/active

# check_quality.py 최종 확인
python -X utf8 tools/check_quality.py refined_vault/active
```

---

## 잔존 이슈

| # | 항목 | 수량 | 조치 |
|---|------|------|------|
| ① | 중첩 wikilink (복잡 구조) | 8개 | 수동 수정 필요 |
| ② | 깨진 링크 | 19개 | `626391685_[기획] 난투전...` 파일 없음 — 원본 확인 필요 |

---

## 코드리뷰 결과 (tools/ 15개 파일)

### HIGH 이슈 → 수정 완료

| # | 파일 | 위치 | 내용 | 상태 |
|---|------|------|------|------|
| 1 | `enhance_wikilinks.py` | line 183 | `re.search(title, ...)` — title 미 escape | ✅ 수정 |
| 2 | `strengthen_links.py` | line 119 | `stem.startswith(s[:len(stem)])` 혼동 | ✅ 명확화 |

### MEDIUM 이슈 (미수정)

- `enhance_wikilinks.py` line 182: `\Q...\E` 구문 — Python re 미지원. 단, `pattern` 변수가 사용되지 않는 데드코드이므로 실행 영향 없음
- 5개 파일 공통: `except: pass` 패턴 (bare except)
- `split_large_docs.py`: 섹션 파싱 로직 직관성 개선 여지
- `check_links.py` line 35: 이미지 확장자 하드코딩
- 중복 함수 (`split_frontmatter`, `parse_frontmatter` 등) — `utils/common.py` 통합 권장

---

*세션 종료: 2026-03-23*
