> **18. Graph RAG 최신성 버그 대응**

Graph RAG 봇이 오래된 데이터를 우선 응답하는 버그의 원인과 해결책을 다룬다.

**18.1 원인 진단**

  -------------------- ----------------------------------------------------------------------------------------------------------- ----------------------------------------------------
  **원인**             **메커니즘**                                                                                                **증상**
  PageRank 편향        오래된 문서는 시간이 쌓이며 더 많은 인바운드 링크를 받음. 오래된 연도 허브가 최신 허브보다 PageRank 높음.   "2022년 기준으로..." 같은 구버전 응답
  ~~BFS 진입점 오염~~  ~~currentSituation.md가 낡아 있거나, 거기서 연결된 허브가 오래된 문서를 먼저 가리킴.~~                     **v3.25에서 해소**: currentSituation.md 강제 주입 폐지. 낡은 파일이 자동 주입되지 않으므로 진입점 오염 없음.
  신규 문서 그래프 고립 새 문서가 \_index.md에만 있고 허브에서 역링크가 없으면 BFS가 해당 문서까지 도달 전 토큰 한계에 걸림.      "그런 문서는 없다"는 응답
  outdated 문서 미정리  status: outdated 이지만 superseded\_by 없는 파일은 탐색에 포함되고 최신 문서 경로가 끊겨 있음.            구정보를 최신 정보로 응답
  -------------------- ----------------------------------------------------------------------------------------------------------- ----------------------------------------------------

**18.2 해결책 적용 절차**

다음 4단계를 순서대로 적용한다.

**[1단계] ~~currentSituation.md 최신화~~ (v3.25 폐지)**

> ✅ **v3.25에서 해결됨**: currentSituation.md 강제 주입이 폐지되어 이 단계는 더 이상 필요하지 않다.
>
> currentSituation.md는 PPR 점수로만 검색 결과에 포함된다. 내용이 오래되면
> 자연히 점수가 낮아져 검색에서 밀리므로 낡은 내용이 AI에게 전달되지 않는다.
>
> 최신성 버그 대응은 이제 [2단계]부터 시작한다.

**[2단계] 최신 연도 허브에 "최근 추가" 섹션 생성**

gen\_year\_hubs.py를 \--top 5 옵션으로 실행하면 최신 연도 허브 파일 상단에 "최근 추가" 섹션이 자동 생성된다.

> python gen\_year\_hubs.py \--vault \<active\_dir\> \--top 5

최신 연도 허브 파일 구조:

> \#\# 최근 추가 (최신 5개) ← BFS 우선 탐색
>
> \> 최신 피드백이 필요하면 이 섹션을 먼저 볼 것.
>
> \- \[\[가장\_최근\_파일\]\] (YYYY.MM.DD)
>
> \- ...
>
> \#\# 전체 목록 (N개 · 최신순)
>
> \- \[\[...\]\]

**[3단계] chief persona.md 최신 연도 허브 강조**

chief persona.md 상단(연도 허브 목록보다 위)에 "최근 피드백 (현재 기준)" 섹션을 두어 BFS가 최신 연도 허브를 우선 탐색하게 한다. gen\_year\_hubs.py 실행 시 자동 갱신된다.

> \#\# 최근 피드백 (현재 기준 --- 우선 참조)
>
> \> ⚠️ 가장 최근 이사장 피드백은 아래 링크를 먼저 확인할 것.
>
> \- \[\[회의록\_YYYY\]\] --- **현재 연도 (최신 N개 문서)**
>
> \#\# 연도별 피드백 아카이브
>
> \> 이전 연도 데이터는 참고용. 현재 기준 정보는 위 섹션 우선.
>
> \- \[\[회의록\_YYYY-1\]\] --- YYYY-1년 전체
>
> \- ...

**[4단계] _index.md 상단에 "최근 30일" 섹션 추가**

전체 인덱스 문서도 BFS 탐색 경로에 포함되므로, 앞부분에 최신 문서 목록을 명시적으로 배치한다.

> \#\# 최근 30일 (YYYY-MM-DD \~ YYYY-MM-DD) ← 현재 기준
>
> \> ⚠️ 최신 데이터가 필요하면 이 섹션을 먼저 볼 것.
>
> \#\#\# 이사장 피드백 / 정례보고
>
> \- 📝 \[\[최신\_피드백\]\] (날짜) ← **최신 피드백**
>
> ---
>
> \#\# 전체 인덱스 (월별 그룹 · 날짜 역순)

**18.3 check_outdated.py — 최신성 자동 점검**

→ 스크립트: check\_outdated.py

> python check\_outdated.py \<active\_dir\> \[\--days 30\] \[\--fix\]

  ---------- -------------------------------------------------------------------------------------------------------------------------------------------
  **항목**   **점검 내용**
  ①          status: outdated 인데 superseded\_by 없는 파일 → \--fix 옵션 시 .archive/ 자동 이동
  ②          최근 N일 이내 신규 문서 중 역링크 0개인 고립 파일 탐지 → gen\_year\_hubs.py 재실행 또는 수동 링크 추가 필요
  ③          \_index.md 의 date 필드가 N일 초과 시 경고 → gen\_index.py 재실행 권장
  ④          chief persona.md 연도 허브 순서 점검 → 최신 연도가 맨 위가 아니면 경고
  ---------- -------------------------------------------------------------------------------------------------------------------------------------------

**18.4 정기 실행 주기**

  ---------- -------------------------------------------------------------------------------------------------
  **주기**   **작업**
  매주       gen\_year\_hubs.py \--top 5 실행, check\_outdated.py 실행 → 경고 0건 확인
  매월       \_index.md "최근 30일" 섹션 갱신 (gen\_index.py 개선 후 자동화 예정)
  분기       outdated 파일 전수 점검, \--fix 옵션으로 일괄 .archive/ 이동
  ---------- -------------------------------------------------------------------------------------------------------------------------------------------

> *v3.25: currentSituation.md 매주 수동 갱신 의무 폐지. 강제 주입 방식 폐지로 낡은
> 내용이 검색 오염을 유발하지 않는다.*

**18.5 Confluence 배치 동기화 date 오염 대응**

Confluence 스페이스를 일괄 내보내거나 페이지를 대량 이동하면, Confluence의
`version.when` 필드가 **실제 작성일이 아닌 동기화 실행 날짜로 일괄 덮어씌워진다.**
refine\_html\_to\_md.py는 이 값을 그대로 `date:` 필드로 사용하므로 2021년 문서도
`date: 2026-03-11` 처럼 최근 날짜로 찍힐 수 있다.

**18.5.1 오염 판별**

배치 날짜 오염의 특징: 특정 날짜를 가진 파일이 **5개 이상 집중**된다.

> python check\_outdated.py \<active\_dir\> \--batch-check

또는 수동 확인:

> grep -rh "^date:" active/ | sort | uniq -c | sort -rn | head -20
>
> → 동일 날짜가 10개 이상이면 배치 동기화 날짜로 의심

실제 오염 사례 (refined\_vault 2026-03-13 진단):

  -------------------- ------- -----------------------------------------------------------------
  **오염 날짜**        **수**  **원인**
  `2026-03-11`         329개   최근 Confluence 배치 동기화. 2020\~2025년 문서 포함.
  `2024-02-28`         69개    이전 Confluence 배치 동기화. 2021년 문서 포함.
  `2022-05-17`         14개    그 이전 배치 동기화.
  -------------------- ------- -----------------------------------------------------------------

**파급 효과**: gen\_year\_hubs.py가 오염된 date 기준으로 연도 허브를 생성하면,
`회의록_2026.md` "최근 추가 5개" 섹션에 **2021년 문서**가 올라간다.
BFS가 이 허브를 출발점으로 삼으면 첫 Hop부터 2021년 문서로 탐색 → 구버전 응답.

**18.5.2 수정 절차**

파일명에서 날짜를 추출하여 오염된 `date:` 필드를 덮어쓴다.

**(1) 파일명 날짜 추출 패턴 (우선순위 순)**

  --------------------------------- ------------------------------------- -------------------------
  **파일명 패턴**                   **예시**                              **추출 날짜**
  `[YYYY_MM_DD]` 대괄호 형식        `[2022_02_07] 이사장 피드백.md`       2022-02-07
  `YYYYMMDD` 8자리                  `ProjectA_정례_20240215_캐릭터C.md`      2024-02-15
  `_YYMMDD_` 또는 `_YYMMDD.` 6자리  `ProjectA_캐릭터C연출_250714.md`         2025-07-14
  파일명 내 연도만 (`_YYYY_`)       `정례보고 자료_2022.md`               2022-01-01 (연도만)
  --------------------------------- ------------------------------------- -------------------------

*6자리 패턴(YYMMDD)은 20YY로 해석하되, 2018\~2030 범위에서만 유효로 처리한다.*

**(2) fix\_dates.py 스크립트 실행**

> \# dry-run: 수정 예상 목록만 출력
>
> python fix\_dates.py \<active\_dir\>
>
> \# 실제 수정
>
> python fix\_dates.py \<active\_dir\> \--apply

  --------------------------------- -------------------------------------------------------------------
  **옵션**                          **설명**
  `--batch-dates 2026-03-11,...`    오염으로 간주할 날짜 목록 (쉼표 구분). 기본값: 자동 감지 (5개+)
  `--apply`                         실제 파일 수정. 없으면 dry-run.
  `--no-match-log out.txt`          파일명에서 날짜 추출 실패한 파일 목록 저장 (수동 처리용)
  --------------------------------- -------------------------------------------------------------------

**(3) 추출 실패 파일 처리**

파일명에 날짜가 전혀 없는 경우(Confluence ID만 있는 파일 등):

1.  title 필드에 `[YYYY.MM.DD]` 패턴이 있으면 그 값을 사용
2.  없으면 파일 본문 첫 날짜 언급을 수동으로 확인
3.  그것도 없으면 **연도만 추출 가능한 경우** `YYYY-01-01`로 대략 입력
4.  완전 불명인 경우 `date: unknown` 표시 후 수동 처리 대기

**18.5.3 수정 후 재생성 순서**

date 필드 일괄 수정 후 **반드시** 아래 순서로 재생성한다.

> \# 1. 키워드 주입 (날짜 변경으로 인한 recency boost 재계산 불필요, 링크 확인용)
>
> python inject\_keywords.py \<active\_dir\>
>
> \# 2. 전체 인덱스 재생성
>
> python gen\_index.py \<active\_dir\>
>
> \# 3. 연도별 허브 재생성 (오염된 날짜 기반으로 만들어진 허브 교체)
>
> python gen\_year\_hubs.py \<active\_dir\> \--top 5

*gen\_year\_hubs.py가 올바른 날짜 기준으로 연도 허브를 재생성해야 `회의록_2026.md`
"최근 추가 5개" 섹션에 **실제 최신 문서**가 올라간다.*

**18.5.4 재발 방지**

-   Confluence에서 **페이지 이동·스페이스 재편** 작업 후에는 반드시 check\_outdated.py
    \--batch-check 실행
-   대량 동기화 직후에는 `grep ... | uniq -c | sort -rn` 으로 date 분포를 확인하고
    동일 날짜 10개 이상이면 fix\_dates.py 우선 실행
-   Recency boost(§18)는 `date:` 필드(frontmatter)를 기준으로 작동하며,
    파일 시스템 mtime은 사용하지 않는다. Confluence 동기화 시 mtime이 오늘로
    갱신되어도 `date:`만 올바르면 문제 없다.
-   **HTML 변환 스크립트(refine\_html\_to\_md.py) 날짜 우선순위 설정 확인 (근본 재발 방지)**:
    스크립트가 Confluence HTML 메타에서 날짜를 읽을 때 `created_date`(생성일)를
    `modified_date`(수정일)보다 **반드시 우선 사용**해야 한다.
    수정일은 페이지 이동·재구성 시 오늘 날짜로 갱신되지만, 생성일은 변하지 않기
    때문이다.

    확인 방법: `refine_html_to_md.py` 내 date 결정 로직이
    `date = created_date or modified_date or today` 순서인지 점검.
    `modified_date or created_date` 순서면 반드시 수정한다.

**18.6 검색 파이프라인 mtime 문제 및 Recency Boost (v3.26)**

Confluence 볼트를 일괄 동기화하면 **모든 파일의 파일시스템 mtime이 동기화 시점으로 통일**된다.
이 때문에 mtime 기반 recency boost는 문서 간 차별화가 불가능하다.

실제 사례 (refined\_vault 2026-03-23 진단):

  ------------------- -------
  **mtime**           **수**
  2026-03-23 15:44    전체 1,210개 (active 전부)
  ------------------- -------

> 이사장 피드백 40여 개 파일이 전부 동일 mtime → recency boost 적용해도 순위 변화 없음.

**18.6.1 해결: 파일명 날짜 파싱 (`parseFilenameDate`)**

graphAnalysis.ts에 `parseFilenameDate()` 함수를 추가하여 파일명에서 콘텐츠 작성일을 추출한다.
mtime 대신 이 값을 recency boost에 사용한다.

파싱 우선순위: **파일명 날짜 > frontmatter date > 파일시스템 mtime**

  --------------------------------- ------------------------------------- -------------------------
  **파일명 패턴**                   **예시**                              **추출 날짜**
  `YYYY_MM_DD` / `YYYY-MM-DD`      `_2025_08_18.md`, `[2022_02_07]`      2025-08-18 / 2022-02-07
  `YYYYMMDD` 8자리 (2000\~2039)    `ProjectA_정례_20240215_캐릭터C.md`      2024-02-15
  `YY_MM_DD` 밑줄/하이픈 구분       `25_03_28 시연용.md`                  2025-03-28
  `YYMMDD` 6자리                   `ProjectA_캐릭터C연출_250714.md`         2025-07-14
  --------------------------------- ------------------------------------- -------------------------

**주의**: Notion/Confluence ID가 8자리 숫자인 경우 오파싱 방지를 위해 YYYYMMDD 패턴은
연도 2000\~2039로 제한한다. 미래 날짜는 30일 이내만 허용한다.

**18.6.2 Recency Boost 수식**

> `score *= 1 + 0.1 * exp(-daysOld / 180)`

- 6개월 이내: \~10% 부스트
- 1년 경과: \~3%
- 2년 이상: 거의 0 (기존 점수 유지)

directVaultSearch와 BM25 search 양쪽에 동시 적용된다.

**18.6.3 검증 결과 (200개 시나리오)**

- "이사장님 최근 피드백" → 2026-01-07 문서 5위→**1위**
- "2026년 1월 작업 내용" → 26-01-19 주간업무보고 상위 진입
- 200개 시나리오 97% 통과 (194/200)

**18.6.4 정제 시 주의사항**

- 파일명에 날짜가 없는 문서(Confluence ID만 있는 파일)는 recency boost 미적용.
  fix\_dates.py로 frontmatter date를 올바르게 설정하면 2순위로 적용됨.
- frontmatter date 오염(§18.5)이 있으면 recency boost도 오염됨.
  **fix\_dates.py를 먼저 실행**한 뒤 검색 테스트.
- TFIDF\_SCHEMA\_VERSION이 변경됨 (6→7). 볼트 재오픈 시 BM25 캐시가 자동 무효화되어 재빌드.

**18.6.5 BM25 범용어 과잉 매칭 잔존 이슈**

coverage^0.5 보정으로 대부분 해소되었으나, 아래 패턴은 여전히 발생:

  --------------------------------- -------------------------------------------------------------------
  **쿼리**                          **문제**
  "캐릭터D 지역 설정"                "지역", "설정" 범용어 → 정례보고가 상위 매칭
  "퀘스트 시나리오"                  둘 다 범용어 → "기획\_" 문서에 분산 매칭
  --------------------------------- -------------------------------------------------------------------

향후 PPR rerank 적용 시 해소 예정. 현재 검색에서는 **고유명사 + 범용어** 조합이 가장 효과적.

**17.4 롤백 전략 (git 없는 환경)**

inject\_keywords.py 등 대량 수정 스크립트 실행 후 문제가 발생했을 때의 복원 절차.

**사전 준비 (스크립트 실행 전 필수):**

```bash
# 실행 전 백업 생성 (zip)
python -c "
import shutil, datetime
ts = datetime.datetime.now().strftime('%Y%m%d_%H%M%S')
shutil.make_archive(f'vault_backup_{ts}', 'zip', 'active/')
print(f'백업 생성: vault_backup_{ts}.zip')
"
```

또는 스크립트 내 자동 백업 옵션 사용:
```bash
python inject_keywords.py <active_dir> --backup  # 실행 전 자동 backup/ 폴더 생성
```

**복원 방법:**

| 상황 | 복원 절차 |
|------|-----------|
| 스크립트 실행 직후 이상 발견 | `backup/` 폴더 → `active/` 교체 (스크립트가 --backup 옵션 지원 시) |
| ZIP 백업에서 복원 | `unzip vault_backup_YYYYMMDD_HHMMSS.zip -d active_restored/` |
| 특정 파일만 복원 | `python restore_file.py <stem>` — backup/ 에서 해당 파일만 복사 |
| 중첩 wikilink 발생 | `audit_and_fix.py --fix` (중첩 링크 자동 수정 ①번 항목) |

**스크립트별 복원 명령:**

```bash
# inject_keywords.py 오주입 일괄 복원
# [[target_stem|키워드]] → 키워드 (plain text)
python inject_keywords.py <active_dir> --revert --keyword "퀘스트"

# 특정 날짜 이후 변경된 파일 목록 확인
find active/ -newer vault_backup_20260319_120000.zip -name "*.md"
```

> *git이 없는 환경에서도 스크립트 실행 전 ZIP 백업을 생성하는 것을 팀 표준으로 정한다.
> 백업 파일 보관 기간: 최소 72시간 (문제 발견 후 복원 여유 확보).*

**17.5 멀티 유저 충돌 해결 가이드**

여러 사람이 동시에 vault를 수정할 때 발생하는 충돌 유형과 해결 기준.

**충돌 유형 및 해결 기준:**

| 충돌 유형 | 발생 조건 | 해결 방법 |
|-----------|-----------|-----------|
| Frontmatter 필드 충돌 | 두 사람이 같은 파일의 tags·status 수정 | 나중 수정 기준 (최신 date 우선). status는 더 낮은 것 우선 (active > outdated) |
| 위키링크 중복 주입 | inject_keywords.py를 여러 사람이 각자 실행 | audit_and_fix.py --fix 로 중첩 링크 정리 후 1인 담당자가 재실행 |
| 허브 파일 동시 수정 | _index.md, chief persona.md 동시 편집 | 작업 전 Slack 등으로 점유 선언. 완료 후 gen_index.py 재실행 |
| 파일명 충돌 | 다른 사람이 만든 파일과 stem 중복 | 파일명에 담당자 이니셜 또는 타임스탬프 추가 후 check_quality.py 실행 |

**팀 운영 규칙 (권장):**

1. **대량 스크립트 실행은 1인 담당자가 수행** — inject_keywords.py, enhance_wikilinks.py는 동시 실행 금지
2. **배치 작업 전 팀 공지** — #vault-ops 채널 등에 "inject_keywords 실행 중 — 파일 편집 대기"
3. **공유 허브 파일 잠금** — _index.md, chief persona.md는 수정 시 공지 (currentSituation.md는 v3.25부터 일반 문서로 취급)
4. **주간 동기화** — 매주 월요일 1인이 check_quality.py + gen_index.py를 일괄 실행하고 결과 공유

> *Git/DVC 기반 협업 환경이면 branch별 작업 후 PR 리뷰로 통합하는 것이 가장 안전하다.
> git 없는 환경에서는 위 역할 분리 규칙으로 충돌을 최소화한다.*

**17.4 권장 폴더 구조**

> vault/
>
> ├── active/ \# 현재 문서
>
> │ ├── meeting/ \# 회의록
>
> │ ├── spec/ \# 스펙·설계
>
> │ ├── decision/ \# 의사결정
>
> │ ├── guide/ \# 가이드·매뉴얼
>
> │ └── reference/ \# 참고 자료
>
> ├── .archive/ \# 폐기·구버전 (AI 탐색 제외)
>
> ├── attachments/ \# 이미지·파일 첨부
>
> │ └── \_orphan/ \# 고아 이미지 격리 (MD 참조 없음)
>
> ├── .manual/ \# 정제 스크립트·매뉴얼
>
> │ └── scripts/
>
> ├── currentSituation.md \# vault 루트
>
> └── .obsidian/app.json \# Obsidian 설정
>
**18.7 BM25 토크나이저 한국어 날짜·경어 매칭 버그 (v3.27)**

**증상**: "2026년 1월 28일 이사장 피드백" 검색 시 해당 파일(`666332716_[2026_01_28] 프로젝트A 이사장님 피드백.md`)이 결과에 누락됨.

**원인 3가지**:

1. **'님' 접미사 미처리**: `stemKorean()`의 `KO_SUFFIXES`에 '님'이 없어 쿼리 토큰 `이사장`과 파일명 토큰 `이사장님`이 별개 토큰으로 처리됨.

2. **1자리 월/일 토큰 탈락**: `tokenize()`가 `(\d+)(년|월|일)` → `$1 $2` 분리 후 `filter(t => t.length > 1)` 적용. "1월" → `['1', '월']` → 양쪽 모두 길이 1이라 전부 탈락. 파일명의 `01`과 매칭할 토큰이 없음.

3. **MMDD 복합 패턴 미지원**: 파일명에 `0128`, `01_28` 등 연월일 조합 패턴이 있을 때 쿼리에서 해당 조합 토큰을 생성하지 않음.

**수정 내용** (`mcp/src/state.ts`, `mcp/src/synonyms.ts`):

- `KO_SUFFIXES`에 `님`, `님의`, `님이`, `님을`, `님은`, `님께`, `님도`, `님과` 추가
- `tokenize()`에 날짜 0-패딩 로직 추가: 1자리 숫자+월/일 → 0-패딩 토큰 생성 (`1월` → `01`)
- `expandTerms()`에 MMDD 조합 확장: 0-패딩 숫자 2개 이상 시 월+일 조합 자동 생성

**검증 방법**: `search_bm25` 도구로 "1월 28일 이사장 피드백" 검색 → 해당 파일이 상위 결과에 포함되는지 확인.

> **19. AI 보조 정제 시 컨텍스트 한계 대응**

Claude Cowork, Claude Code 등 AI 도구로 정제 작업을 수행할 경우, **컨텍스트 윈도우 제한** 때문에 다단계 파이프라인이 중간에 끊어지는 문제가 발생한다. 아래 절차를 따르면 세션이 초기화되어도 작업을 안전하게 이어갈 수 있다.

**19.1 문제 현상**

- 대부분의 AI 도구(Claude 포함)는 컨텍스트 용량 초과 시 새 세션을 시작하며, 이전 세션의 작업 상태를 기억하지 못한다.
- 14단계 파이프라인을 한 세션에서 전부 완료하는 것은 현실적으로 불가능하다.
- AI가 자동으로 "다음 단계"로 넘어가지 못하고 멈추는 경우가 발생한다.

**19.2 세션 재시작 시 표준 복구 절차**

새 세션이 시작되면 **반드시 아래 순서로 작업 상태를 파악**한 뒤 작업을 이어간다.

  ------ -------------------------------------------------------------------
  **순서** **작업**
  1      check\_quality.py 실행 → 현재 품질 지표 확인 (어디까지 진행됐는지 파악)
  2      본 매뉴얼 전체 검토 → 구현된 스크립트와 미구현 항목 대조
  3      .tools/ 폴더 목록 확인 → 생성된 스크립트 목록으로 진행 상황 재확인
  4      미구현 항목 중 우선순위 높은 것부터 재개
  ------ -------------------------------------------------------------------

**19.3 세션 이어받기용 프롬프트 템플릿**

새 세션 시작 시 아래 프롬프트를 AI에게 전달하면 빠르게 상태를 파악하고 작업을 이어간다.

> "Graph RAG 데이터 정제 매뉴얼 v3.27을 읽고 현재 구현 상태를 파악한 다음, 미구현 항목을 우선순위 순서대로 진행해줘.
> check\_quality.py로 현재 품질 지표를 먼저 확인하고(헤딩 비율 99% 기준), .tools/ 폴더의 스크립트 목록도 확인해줘.
> v3.27 변경사항: BM25 토크나이저 한국어 날짜·경어 매칭 버그 3건 수정(§18.7). KO\_SUFFIXES에 '님' 추가, tokenize()에 월/일 0-패딩 복원, expandTerms()에 MMDD 조합 확장. .archive/ status 일괄 정규화, active260323/ 프론트매터 정규화 완료. Confluence 동기화 후 fix\_dates.py 실행 권장(§18.5·§18.6 참조)."

**19.4 중간 체크포인트 기록 (권장)**

장기 정제 작업 중에는 아래 내용을 메모해두면 세션 재시작 후 빠르게 복구된다.

- 마지막으로 완료한 파이프라인 단계 (§번호)
- 미완료 단계 목록
- check\_quality.py 마지막 실행 결과 (품질 지표 수치)
- 이슈가 있었던 파일명이나 패턴

**19.5 자동화 우선 원칙**

수동 수정이 필요한 경우라도 **반드시 스크립트화하여 .tools/에 저장**해두어야 한다. 세션이 끊어졌을 때 수동 작업은 재현이 불가능하지만, 스크립트는 언제든 재실행할 수 있다.

**20. 매뉴얼 관리 & 업데이트 절차**

매뉴얼에 신규 규칙·버그 패턴·스크립트 정보가 추가될 때마다 MD 원본과 DOCX 배포본을 동시에 갱신해야 한다.

**20.1 버전 번호 규칙**

  -------------- ----------------------------------------------------------
  **종류**       **기준**
  패치 (3.x)     기존 섹션 내 규칙 추가·수정·버그 패턴 추가·스크립트 갱신
  마이너 (x.0)   새 섹션(§N) 신설 또는 파이프라인 단계 추가
  -------------- ----------------------------------------------------------

**20.2 업데이트 체크리스트**

매뉴얼을 수정할 때마다 아래 항목을 순서대로 이행한다.

  ------ --------------------------------------------------------------------------
  **순서** **작업**
  ①      본문 해당 섹션 수정 (MD 파일)
  ②      §19.3 이어받기 프롬프트 내 버전 번호를 현행 버전으로 갱신 (§20.4 참조)
  ③      변경이력 테이블 최상단에 새 행 추가 (형식: `vX.Y | 내용 요약 | YYYY-MM-DD`)
  ④      파일명을 새 버전으로 변경 (`Graph RAG 데이터 정제 매뉴얼 vX.Y.md`)
  ⑤      DOCX 동기화 — §20.3 절차 참조
  ------ --------------------------------------------------------------------------

> **주의**: ①\~④는 MD 파일 단독으로 완결하고, ⑤를 별도로 진행한다. MD와 DOCX
> 수정을 동시에 교차하면 version mismatch가 발생할 수 있다.

**20.3 MD → DOCX 동기화 절차**

**방법 A: pandoc 자동 변환 (권장)**

MD → DOCX를 가장 안전하게 변환하는 방법이다. XML 수동 편집 오류를 방지한다.

```bash
# 기본 변환 (스타일 참조 없음)
pandoc "Graph RAG 데이터 정제 매뉴얼 v3.23.md" \
  -o "Graph RAG 데이터 정제 매뉴얼 v3.23.docx"

# 기존 DOCX 스타일 템플릿 적용 (권장 — 기존 포맷 유지)
pandoc "Graph RAG 데이터 정제 매뉴얼 v3.23.md" \
  --reference-doc="Graph RAG 데이터 정제 매뉴얼 v3.22.docx" \
  -o "Graph RAG 데이터 정제 매뉴얼 v3.23.docx"
```

pandoc 설치:
- Windows: `winget install JohnMacFarlane.Pandoc` 또는 [공식 다운로드](https://pandoc.org/installing.html)
- macOS: `brew install pandoc`
- Ubuntu: `sudo apt install pandoc`

변환 후 검증:
```bash
# 주요 신규 문구가 포함됐는지 확인
pandoc "Graph RAG 데이터 정제 매뉴얼 v3.23.docx" -t plain | grep "Tesseract"
pandoc "Graph RAG 데이터 정제 매뉴얼 v3.23.docx" -t plain | grep "멀티 유저"
```

**방법 B: XML 수동 편집 (pandoc 미사용 환경)**

Claude Cowork(또는 Claude Code)에서 DOCX 스킬을 사용하여 아래 절차로 진행한다.

  ------ -----------------------------------------------------------------------
  **순서** **작업**
  ①      `scripts/office/unpack.py` 로 기존 DOCX 언패킹 → `/manual_unpacked/`
  ②      `word/document.xml` 에서 변경된 섹션을 찾아 XML 편집
  ③      `scripts/office/pack.py` 로 리패킹 → `vX.Y.docx` 저장
  ④      pandoc 추출 후 키워드 grep 검증 (주요 신규 문구가 포함됐는지 확인)
  ------ -----------------------------------------------------------------------

> 편집 시 `<w:t>` 태그 안의 텍스트만 수정하며, `<w:pStyle>` 값은 기존 단락
> 스타일을 유지한다. 신규 단락 삽입 시 인접 단락 블록을 복사하여 스타일을 상속한다.

> *방법 A(pandoc)를 우선 시도하고, 스타일 불일치가 심각한 경우에만 방법 B를 사용한다.
> XML 수동 편집은 태그 구조 오류 시 DOCX가 열리지 않을 수 있으므로 수정 전 DOCX 원본을 백업한다.*

**20.4 §19.3 프롬프트 버전 번호 갱신 규칙**

§19.3의 이어받기 프롬프트 템플릿에는 매뉴얼 버전 번호가 하드코딩되어 있다.
매뉴얼 업데이트 시 **반드시** 이 번호도 현행 버전으로 변경한다.

> 갱신 대상 문구: `"Graph RAG 데이터 정제 매뉴얼 vX.X을 읽고..."`

> **변경 이력**

  ---------- -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
  **버전**   **내용**
  v3.27      BM25 토크나이저 한국어 날짜 검색 버그 수정 3건 --- ① KO_SUFFIXES에 '님' 접미사 추가: "이사장" 쿼리가 파일명 "이사장님"과 매칭 실패하던 버그 해소 (stemKorean에서 '님' 제거 → 양방향 매칭). ② tokenize()에 날짜 0-패딩 복원: "1월"→['1','월'] 양쪽 len>1 필터 탈락 → "01" 패딩 토큰 추가 생성. "1월 28일" 검색 시 파일명 "01_28" 매칭 가능. ③ expandTerms()에 MMDD 조합 확장: 0-패딩 숫자 2개 이상 시 월+일 조합("0128") 자동 생성. 볼트 데이터 수정: .archive/ 252개 파일 status: active→archived 일괄 변경, active260323/ 30개 파일 프론트매터 정규화(created→date, type/status/origin 추가), jira-members.md 프론트매터 보완. 2026-04-07.
  v3.26      §18.6 검색 파이프라인 mtime·recency 개선 --- ① mtime bulk sync 문제 문서화: Confluence 일괄 동기화 시 전체 1,210개 파일 mtime이 동일 시점(2026-03-23 15:44)으로 덮어씌워져 recency 무력화. ② parseFilenameDate() 구현: 파일명 날짜 패턴 4종(YYYY\_MM\_DD·YYYYMMDD·YY\_MM\_DD·YYMMDD) 파싱 → getContentDate() 우선순위 체계(파일명 > frontmatter > mtime). Notion ID 8자리(59760421 등) 오탐 방지를 위해 연도 2000-2039 제한 + 미래 30일 마진. ③ BM25/directVaultSearch recency boost: score \*= 1 + 0.1 \* exp(-daysOld / 180). 6개월 \~10%, 1년 \~3%, 2년+ \~0%. ④ TFIDF\_SCHEMA\_VERSION 6→7 캐시 무효화. ⑤ 200 시나리오 검증: 194/200 (97%) 통과. §18.6.5 BM25 범용어 잔존 이슈 기록. 2026-03-25.
  v3.25      검색 품질 핵심 개선 5종 --- ① currentSituation.md 강제 주입 폐지: graphRAG.ts에서 situationPrefix 블록 제거. PPR 점수 기반 일반 허브 문서로 전환. 낡은 내용이 검색 오염을 유발하는 버그 해소. ② 헤딩 보유 비율 기준 80%→99%: s06\_optimization §10.1·s01\_overview KPI·check\_quality.py WARN 기준 일괄 수정. ③ BM25/TF-IDF 캐시 즉시 무효화: Edit Agent가 파일 편집 완료 즉시 IndexedDB 캐시 삭제 → 다음 검색 시 자동 재빌드. 볼트 재오픈 불필요. ④ check\_quality.py EditAgentLog 자동 노출: Edit Agent 사이클 완료 시 WARN 항목·총 이슈 건수 자동 표시. ⑤ Edit Agent DEFAULT\_SECTIONS에 s05·s06 추가: 링크 강화·PPR 최적화 규칙 포함. POST\_SYNC\_SCRIPTS에 strengthen\_links.py 추가. §18.1 BFS 진입점 오염 해소. §18.2 [1단계] 폐지. §18.4·§17.5 갱신. §19.3 프롬프트 v3.25 갱신. 2026-03-23.
  v1.0       초기 버전 --- 11단계 정제 방법론. 삭제·격리·수정·보강 기준, 위키링크 강화, 섹션 헤딩, PageRank 최적화, 품질 체크리스트. 2026-03-01.
  v2.0       외부 데이터 수집·변환, 대용량 문서 분할, 이미지 처리 가이드 추가. 14단계로 확장. 스크립트 목록 통합. HTML·PDF 2종 변환 가이드. 2026-03-03.
  v3.0       inject\_keywords.py placeholder 방식(v2) 반영. audit\_and\_fix.py 신규 추가. 연도별 허브 구조, chief 태그 관리, 3중 대괄호 false positive 해설 추가. 17단계로 확장. 2026-03-05.
  v3.1       13.3 MANUAL\_MAP 패턴 보강. Confluence ID 접미사 불일치 4가지 예외 패턴(날짜 대괄호·따옴표·백슬래시·날짜+ID 혼합) 설명 및 예시 테이블 추가. 2026-03-05.
  v3.2       14단계 추가 --- 데이터 일원화 & 허브 구조 강화. 이사장 피드백 연도별 허브 9개(2018--2026) 생성, 223개 역링크 주입, chief persona "연도별 피드백 아카이브" 테이블 갱신. 빈 문서 4개 .archive/ 격리. 고립 노드 245개 → 0개. 2026-03-05.
  v3.3       15단계 추가 --- 링크 품질 개선 & 인덱스 구조 재편. \_스킬\_모음/\_리소스\_모음 5개 파일 중복 링크 통합 제거(최대 43→4회). 주간업무보고 11개 파일 본문 중복 링크 제거. 캐릭터별 인덱스 10개(\_index\_캐릭터\_\*) + 맵별 인덱스 2개(\_index\_점령전/난투전) 신규 생성. \_index.md 40,888자 → 2,264자 허브형 재작성. 2026-03-05.
  v3.4       16단계 추가 --- chief persona 허브 폭발 해소 & .anim 기술 데이터 분리. chief persona 인바운드 링크 254개 → 9개(연도 허브 경유만 유지). 캐릭터 허브 6개에서 .anim 분리 → \_tech\_\*\_애니메이션\_리스트.md 6개 신규 생성(총 756개 anim 분리). 고립 노드 9개 → 0개. 총 파일 572개. 2026-03-05.
  v3.5       17단계 추가 --- 의미없는 데이터 추가 정제. 기술 레퍼런스 4개 technical 태깅(데이터테이블/사운드 old/블록 목록 2개). Confluence 압축 테이블 2개(1,455개 항목) 마크다운 테이블 재구성. 이미지 파일명 노이즈 120개 파일 1,564개 일괄 제거. 고립 노드 0 유지. 2026-03-05.
  v3.6       실전 정제 경험 반영 --- ① 스텁 자동 판단 기준(3.1.1): frontmatter·H1·원본링크 제외 50자 미만. ② archive 이동 후 dead link 정리 절차 추가(3.2.1). ③ 고립 노드 fallback 처리 명문화(3.2.2). ④ 괄호 포함 파일명 링크 절단 버그 패턴 및 자동 복구 방법 추가(13.2.1). ⑤ 이미지/문서 링크 분리 점검 방법 추가(13.1.1). ⑥ chief 태그 자동화 로직 및 연도별 허브 자동 생성 방법 추가(11.3.1-2). ⑦ 스크립트 목록 실제 사용 스크립트 기준으로 갱신(17.3). 2026-03-11.
  v3.24      inject\_keywords.py v3 자동 모드 전환 --- ① inject\_keywords.py v3: 실행 시 \_index.md 자동 분석으로 키워드 맵 동적 구성 (KEYWORD\_MAP 수동 관리 불필요). KEYWORD\_MAP\_MANUAL로 수동 오버라이드만 유지. MIN\_STEMS≥3·MIN\_FREQ≥3·MAX\_RATE≤15% 3단계 필터·STOPWORDS 내장·대표 허브 자동 선택. ② gen\_keyword\_map.py 신규 추가: 자동 분석 결과 미리보기 및 진단 전용 (파이프라인 실행에 불필요). ③ POST\_SYNC 파이프라인 순서 수정: gen\_index → inject\_keywords (기존 inject\_keywords → gen\_index 역전). ④ §9 전면 개정: §9.0 자동 모드 신설, §9.1 KEYWORD\_MAP\_MANUAL 수동 오버라이드로 축소, §9.4 gen\_keyword\_map.py 진단 도구 안내, §9.5 별칭 처리, §9.6 STOPWORDS 기반 범용어 제외. ⑤ §17.1.1 키워드 역전파 절차 2단계로 단축 (수동 KEYWORD\_MAP 등록 제거). ⑥ §17.1.4 대량 배치 순서 ③ gen\_index → ④ inject\_keywords 로 수정. ⑦ §17.3 스크립트 목록 inject\_keywords.py v3·gen\_keyword\_map.py 설명 갱신. check\_links.py·check\_outdated.py·check\_quality.py 신규 추가. 2026-03-19.
  v3.23      구조·운영 개선 6종 추가 --- ① §목차 신설 (20개 섹션 링크 테이블). ② §2.1-2.2 Mermaid 파이프라인·BFS 흐름도 추가. ③ §4.2.0 Tesseract 플랫폼별 설치 가이드 신설 (Windows·macOS·Linux) + 대용량 PDF 타임아웃·다국어 혼합 OCR 대응. ④ §9.6.1 KEYWORD_MAP 자동 판단 알고리즘 추가 (keep/review/remove 자동 분류). ⑤ §17.1 신규 문서 추가 단계별 자동화 가능 여부 표 추가. ⑥ §17.4 롤백 전략 신설 (git 없는 환경에서 ZIP 백업·복원 절차). ⑦ §17.5 멀티 유저 충돌 해결 가이드 신설 (충돌 유형·해결 방법·팀 운영 규칙). ⑧ §20.3 pandoc 자동 변환 방법 추가 (XML 수동 편집 대체). §19.3 이어받기 프롬프트 v3.23 갱신. 2026-03-19.
  v3.22      §18.5 Confluence 배치 동기화 date 오염 대응 신설 --- refined\_vault 진단(2026-03-13)에서 발견된 버그: Confluence 스페이스 일괄 내보내기 시 version.when이 동기화 실행일로 덮어씌워져 2021년 문서도 date:2026-03-11로 찍힘. 결과적으로 gen\_year\_hubs.py가 2021년 문서를 "최신"으로 분류하고 BFS가 구버전 응답. 오염 판별법(date 분포 집계), fix\_dates.py 스크립트 사용법(파일명 패턴 4종·dry-run·--apply 옵션), 수정 후 재생성 순서(gen\_index → gen\_year\_hubs → currentSituation 수동 갱신), 재발 방지(배치 동기화 후 즉시 점검) 추가. §19.3 이어받기 프롬프트 v3.22 갱신. 2026-03-13.
  v3.21      ⚠️ **BREAKING**: "이사장님" 키워드 복원 (v3.20 제거 역행) --- v3.20에서 제거한 "이사장님" → chief persona 링크를 재주입. 제거 후 AI가 chief persona 허브를 BFS로 탐색하지 못하는 문제 확인. 원인: frontmatter related: 는 구조 메타데이터이며 Graph RAG BFS 탐색 엣지가 아님 — body [[chief persona|이사장님]] 링크가 없으면 탐색 불가. 189개 파일에 링크 재주입. §9.5 별칭 예시 복원. §9.6 판단 기준 개정: "고유명사·캐릭터급 허브는 등장률 무관 유지 — 범용 게임 용어(퀘스트 등)만 제거 대상". §17.3 inject_keywords.py 복원 이력 추가. §19.3 이어받기 프롬프트 v3.21 갱신. 2026-03-12.
  v3.20      KEYWORD\_MAP 범용어 2차 정제 및 품질 감사 확장 --- ① "이사장님" 키워드 KEYWORD\_MAP 제거: chief 파일은 frontmatter related:[회의록\_YYYY, chief persona]로 구조 연결 완료, body 주입 불필요. 기존 주입 202개 plain text 복원, 195개 파일 수정. ② check\_keyword\_density.py 신규 추가(§9.6): inject\_keywords.py 실행 전 키워드 문서 등장 비율 사전 점검. 임계값 15% 초과 키워드 ⚠️ 경고. ③ check\_quality.py 12개 항목으로 확장: ⑪ speaker 필드 누락(0개 — 전파일 완료) / ⑫ chief 태그이지만 related 없음(6개 — hub 파일 자체는 정상) / graph\_weight 분포 출력 추가. ④ §9.5 존칭 별칭 예시를 제거 사례 주석으로 전환. ⑤ §13.1 ⑪/⑫ 항목 재정의 + graph\_weight 분포 확인 섹션 추가(13.1.1). §17.3 스크립트 목록 갱신(check\_keyword\_density.py 추가, check\_quality.py 12개 항목으로 갱신). §19.3 이어받기 프롬프트 v3.20 갱신. 2026-03-12.
  v3.19      링크 노이즈 제거 3종 실행 --- ① "퀘스트" 키워드 KEYWORD\_MAP 제거: 범용어(41.1% 문서 과주입), 기존 주입 530개 plain text 복원, 505개 파일 수정. ② 관련 문서 섹션 반복 노이즈 링크 제거: 26개 자동생성 허브가 10~382개 문서에 균일 주입된 3,685개 링크 plain text 복원, 871개 파일 수정. ③ 결과: normal 문서 비이미지 링크 평균 3.4개/중앙값 2개로 정상화. §17.3 inject\_keywords.py 범용어 제외 지침 추가. §19.3 이어받기 프롬프트 v3.19 갱신. 2026-03-12.
  v3.18      링크 밀도 관리 체계 도입 --- ① 볼트 링크 밀도 분석(평균 44.5개/문서, 중앙값 14개). ② year hub 역링크 body→frontmatter 이동: gen\_year\_hubs.py v3.8 개정, 724개 파일 related: [회의록\_YYYY, chief persona] 마이그레이션. ③ graph\_weight 필드 신설: 100-499링크→low(92개), 500+링크→skip(10개) 자동 표시. ④ §4.0 frontmatter 스키마에 graph\_weight·related 필드 추가. §17.3 gen\_year\_hubs.py v3.8 설명 갱신. §19.3 이어받기 프롬프트 버전 v3.18 갱신. 2026-03-12.
  v3.17      3종 볼트 품질 정제 실행 --- ① inject\_keywords.py placeholder 정규식 버그 수정: \[^\\]\]+ → (?:[^\\]]|\\](?!\\]))+으로 교체, \] 포함 파일명 링크 내 키워드 다중 주입 손상 방지. 손상 링크 132개 파일(2~3중 중첩 패턴) 복구. ② speaker 필드 일괄 주입: 1,186개 전 파일에 tags 기반 자동 추정 주입(chief\_director 346개 / art\_director 272개 / tech\_director 187개 / design\_director 214개 / unknown 167개). inject\_speaker.py 생성·저장. ③ 소형 파일 3개 보강: description 필드 추가 및 \\\{backslash\} 아티팩트 정리. §17.3 스크립트 목록 inject\_speaker.py 추가. §19.3 이어받기 프롬프트 버전 v3.17 갱신. 2026-03-12.
  v3.16      README.md 기술 스펙 반영 5항목 --- ① §4.0 Frontmatter에 speaker 필드 추가(PPR speaker affinity 재순위화 활용). ② §4.0.2 날짜 파일명 권장 패턴 신설(\[YYYY.MM.DD\] 형식 → directVaultSearch 자연어 매칭). ③ §10.3 PPR 컨텍스트 예산 표 신설(1위-3위 1500자/4위-8위 900자/9위+ 500자). ④ §13.1 체크리스트 ⑫ 팬텀 노드 항목 추가. ⑤ §17.1.5 TF-IDF 자동 재인덱싱 안내 신설(mtime fingerprint 기반, 볼트 재오픈으로 자동 반영). §19.3 이어받기 프롬프트 버전 v3.16 갱신. 2026-03-12.
  v3.15      KEYWORD\_MAP 미등록 고빈도 키워드 4개 추가 및 역전파 실행 --- ① 이사장님(링크율 0%) → chief persona 허브 별칭 등록. ② 텔모칸(링크율 12%) → 세계관 테우아칸 허브 등록. ③ 퀘스트(링크율 66%) → 퀘스트컨텐츠 1막 초반 허브 등록. ④ 샤마쉬 → 점령전1 비트 샤마쉬 레벨 고려사항 허브 등록. inject\_keywords.py 실행 후 718개 파일 변경. §9.1 이사장 예시 stem 수정(chief persona(0.1.0) → chief persona). §9.5 존칭 별칭 예시 추가. §19.3 이어받기 프롬프트 버전 v3.15 갱신. 2026-03-12.
  v3.14      §4.0.1 Confluence 첨부 파일 출처 기록 규칙 신설 --- PPT·XLSX·DOCX 등 Confluence 첨부 파일 변환 시 source 필드에 부모 페이지 URL 기재 규칙 명시. 본문 ## 개요 첫 줄 wikilink 패턴 추가(부모 페이지 변환 여부에 따른 3가지 처리 방법). §4.8 형식별 비교 요약 테이블에 "Confluence 출처 기록" 열 추가. §19.3 이어받기 프롬프트 버전 v3.14 갱신. 2026-03-12.
  v3.13      신규 문서 추가 워크플로우 전면 보강 --- ① §17.1 신규 문서 추가 절차 6단계 재정비. ② §17.1.1 키워드 역전파 5단계 절차 신설. ③ §17.1.2 \_index.md 갱신 기준표 신설. ④ §17.1.3 currentSituation.md 갱신 기준표 신설. ⑤ §17.1.4 대량 문서 배치 추가 시 7단계 순서 신설. ⑥ §9.4 KEYWORD\_MAP 등록 기준표 신설(등록 대상/제외 대상). ⑦ §9.5 유사어·별칭 처리 신설. ⑧ §6.3.1 신규 태그 추가 및 태그\_정의서 갱신 절차 신설. ⑨ §5.3 허브 파일 크기 관리 기준 신설(인바운드 50개/300줄/outbound 30개 임계값). ⑩ §3.2.3 아카이브 판단 복합 기준표 신설. 2026-03-12.
  v3.12      §20 매뉴얼 관리 & 업데이트 절차 신설 --- 버전 번호 규칙(패치/마이너), 업데이트 5단계 체크리스트, MD→DOCX 동기화 절차, §19.3 프롬프트 버전 번호 갱신 규칙. §19.3 이어받기 프롬프트 버전 번호 v3.8 → v3.12 수정. 2026-03-12.
  v3.11      실전 정제 경험 반영 (5항목) --- ① §3.1.1 테이블 셀 이미지 텍스트 오분류 버그 패턴 추가 및 audit\_and\_fix.py 자동 교체 권장. ② §6.1.1 origin:generated 파일(chief persona·\_index·currentSituation·회의록\_YYYY·태그\_정의서) source:generated 규칙 명시, check\_quality.py 예외 처리 가이드 추가. ③ §13.1 ⑪ 고아 첨부 파일 점검 항목 신설, attachments/\_orphan/ 격리 패턴 추가. ④ §14.1 currentSituation.md 위치를 vault 루트 단일 파일로 명확화(active/ 복사본 제거 지침). ⑤ §17.4 폴더 구조에 attachments/\_orphan/ 추가. 2026-03-12.
  v3.10      미구현 스크립트 4종 구현 완료 --- txt\_to\_md.py(§4.6) TXT→MD 변환·frontmatter 자동 생성·스텁 경고. md\_normalize.py(§4.7) 기존 MD frontmatter 정규화·origin:md 추가·이미지 링크 점검. extract\_images.py(§4.2) PDF 이미지 추출 독립 스크립트·pymupdf 기반·소형 아이콘 필터·PNG 통일. split\_large\_docs.py(§5) 대용량 문서 허브-스포크 분할·200줄+ 파일 탐지·## 헤딩 단위 분리·역참조 링크 자동 생성. §17.3 스크립트 목록 갱신(총 24개). 2026-03-12.
  v3.9       섹션 19 추가 --- AI 보조 정제 시 컨텍스트 한계 대응. 세션 재시작 복구 절차 4단계, 이어받기 프롬프트 템플릿, 중간 체크포인트 기록 방법, 자동화 우선 원칙 추가. scan\_cleanup.py(§3) · check\_links.py(§13.1.1) 신규 추가. check\_quality.py 10개 항목으로 확장(⑤~⑩ 추가). audit\_and\_fix.py MANUAL\_MAP 및 F1 3열림+2닫힘 패턴([[[stem]]) 수정 추가. 태그\_정의서.md 생성(21개 태그 정의). 2026-03-12.
  v3.8       섹션 4 전면 재작성 --- 원본 파일 변환 대상을 HTML·PDF 2종에서 PPTX·XLSX·DOCX·TXT·MD 포함 7종으로 확장. §4.0 공통 원칙(출력구조·frontmatter·이미지 파일명 규칙) 신설. §4.3 PPTX·§4.4 XLSX·§4.5 DOCX·§4.6 TXT·§4.7 MD 정규화 각 처리 가이드 추가. §4.8 형식별 비교 요약 테이블 추가. 파이프라인 표 2단계 업데이트. 스크립트 목록에 pptx\_to\_md·xlsx\_to\_md·docx\_to\_md·txt\_to\_md·md\_normalize 5개 추가. 2026-03-11.
  v3.7       Graph RAG 최신성 버그 대응 추가(섹션 18) --- ① 원인 진단 4가지(PageRank 편향·BFS 진입점 오염·신규 문서 고립·outdated 미정리). ② 해결책 4단계 절차: currentSituation.md 최신화·최신 연도 허브 "최근 추가" 섹션·chief persona 최신 허브 강조·_index.md 상단 "최근 30일" 섹션. ③ check_outdated.py 신규 추가(4개 항목 자동 점검). ④ gen_year_hubs.py --top N 옵션 추가(최신 연도 허브 상단 노출). ⑤ 정기 실행 주기 표(18.4). 2026-03-11.
  ---------- -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
