# Brand assets and user manual

Recovered on 2026-09-15 from the user's selected Vercel deployment:
https://strata-sync-n4aqo8ejq-mirokims-projects.vercel.app/

Deployment: `dpl_DayKkjRLZWnE8SHdgoKmombvEpnC` (2026-09-13 15:16 KST).
Vercel records this as a CLI deployment with uncommitted changes (`gitDirty=1`).

The SVG assets, brand HTML pages, submission cover, and `manual.html` were
recovered byte-for-byte through the authenticated deployment-files API.
`strata-sync-icon.svg` and `strata-sync-wordmark.svg` form the selected app logo.
The numbered symbols and light/dark logos preserve the alternative designs.

The editable React components were reconstructed from the deployed bundle:
`BrandLogo` and the Settings user-manual tab with its MCP shortcut.
The current repository's loading and synchronization behavior remains in place.

The app serves `public/manual.html`; keep `docs/manual.html` in sync when editing it.
